import { DollerCurrencyPipe } from './doller-currency.pipe';

describe('DollerCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new DollerCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
